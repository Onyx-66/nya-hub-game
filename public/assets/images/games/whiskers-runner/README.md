# Whiskers Runner — Icon Asset

`icon.svg` — flat vector cat-face mark on an amber gold (#f59e0b) rounded-square background,
matching the visual style of other Nya Hub game icons. Used as the fallback/static icon where
the `lucide:Cat` iconPath isn't rendered directly (e.g. app manifest, share cards, hub tile
thumbnails). No external assets or licensing required — original flat-shape artwork.
