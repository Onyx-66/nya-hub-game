# Super Cat Jump — Icon Asset

`icon.svg` — flat vector cat-face mark leaping over a ground platform, on an emerald green
(#10b981) rounded-square background, matching the visual style of other Nya Hub game icons.
Used as a fallback/static icon where `lucide:Footprints` isn't rendered directly (app manifest,
share cards, hub tile thumbnails). Original flat-shape artwork, no external assets or licensing
required.
